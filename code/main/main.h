#ifndef MAIN_H
#define MAIN_H

// Function that handles and runs the entire Neural Network
int run_neural_network(void);

// Function to process the command line arguments
int process_command_args(int, char*[]);

#endif //MAIN_H